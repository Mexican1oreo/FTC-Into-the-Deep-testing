package org.firstinspires.ftc.teamcode.Util;

public class RobotStates {
    public enum Arm {
        DOWN,
        UP
    }

    public enum Claw {
        OPEN,
        CLOSED
    }

    public enum LinearSlide {
        START_POS,
        LOW_SCORE,
        HIGH_SCORE
    }

    public enum wrist {
        FLOOR,
        SCORE
    }
}
